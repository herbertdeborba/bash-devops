#!/bin/bash

DATALOG=`date +"%Y-%m-%d"`

#tail -f ${AS_BASE_DIR}/${INSTANCIA}/logs/host-manager.${DATALOG}.log ${AS_BASE_DIR}/${INSTANCIA}/logs/liferay.${DATALOG}.log ${AS_BASE_DIR}/${INSTANCIA}/logs/localhost.${DATALOG}.log ${AS_BASE_DIR}/${INSTANCIA}/logs/localhost_access_log.${DATALOG}.txt ${AS_BASE_DIR}/${INSTANCIA}/logs/catalina.out
